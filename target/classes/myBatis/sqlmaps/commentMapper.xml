<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Comment">
<!--   리스트 조회 -->
    <select id="commentList"
            parameterType="Integer"
            resultType="com.green.vo.CommentVo">
        SELECT
            _id,
            username,
            time,
            content
        FROM
            comment_tb
        WHERE
            content_id = #{content_id}

    </select>

<!-- 리스트 10개씩 -->
    <select id="commentListpage" parameterType="hashMap" resultType="com.green.vo.CommentVo">
        SELECT
            _id,
            username,
            time,
            content
        FROM comment_tb
        order by _id desc
            limit #{displayPost}, #{postNum}
    </select>
<!--댓글 카운트-->
    <select id="commentCount" resultType="int">
        SELECT
            count(_id)
        FROM comment_tb
        WHERE content_id=#{content_id}
    </select>
<!--코멘트 쓰기-->
    <insert id="writeComment">
        INSERT INTO  comment_tb( content_id, username, content, time)
        VALUES (#{content_id}, #{username}, #{content}, NOW())
    </insert>
<!--코멘트 수정-->
    <update id="commentUpdate">
        UPDATE comment_tb
        SET content =  #{content}
        Where    username = #{username}
          AND _id      = #{_id}
    </update>

<!--코멘트 삭제-->
    <delete id="commentDelete">
        DELETE FROM comment_tb
        WHERE _id  = #{_id}
    </delete>

</mapper>